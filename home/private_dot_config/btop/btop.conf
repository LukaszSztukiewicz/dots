#? Config file for btop v. 1.x
# Only overrides we care about; btop fills the rest with defaults.
# Note: btop rewrites this file on exit. If you change settings interactively,
# `chezmoi re-add ~/.config/btop/btop.conf` to capture them.

color_theme = "tty"
theme_background = False
truecolor = True
